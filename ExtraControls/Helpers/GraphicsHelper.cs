using System.Drawing;

namespace ExtraControls.Helpers
{
    public static class GraphicsHelper
    {
        public static Graphics DrawRoundedRectangle(this Graphics gfx, Color color, int penThickness, int cornerRadius)
        {
            return gfx;
        }

        public static Graphics FillRoundedRectangle(this Graphics gfx, Color, int cornerRadius)
        {
            return gfx;
        }
    }
}
